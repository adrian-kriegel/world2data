# Environment & secrets
.env
.env.*

# Virtual environments
.venv/
.venv-world2data/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.pytest_cache/

# MASt3R (large subrepo + checkpoints)
mast3r/

# ---------------------------------------------------------------
# Data directory (large / generated artifacts, keep structure only)
# ---------------------------------------------------------------
# Videos (large, not committed)
data/inputs/*.mp4
data/inputs/*.avi
data/inputs/*.mov
# Model weights (large, downloaded on first run)
data/models/*.pt
data/models/*.pth
data/models/*.bin
data/models/*.safetensors
data/models/*.onnx

# HuggingFace cache (SAM3, YOLO weights, etc.)
.cache/
*.ckpt

# All generated outputs (USD scene bundles, PLY, parquet, RRD)
data/outputs/*
# But keep the directory structure markers
!data/inputs/.gitkeep
!data/outputs/.gitkeep
!data/models/.gitkeep

# USD layered scene outputs
scene/

# Session layers (per-user, not tracked)
**/layers/99_session.usda

# IDE
.vscode/
.idea/

# OS
Thumbs.db
.DS_Store

# Temp
*.tmp
w2d_keyframes_*/
w2d_cache_*/

# Testenvironment generated artifacts
testenvironment/**/outputs/
testenvironment/**/.venv/

# External local tooling (not part of project source)
tools/
tolls/
